/*
 * placa.cpp
 */


#include <iostream>
using namespace std;

int main(int argc, char **argv)
{
    float placa, kla , nd, snd;
    int x;
    x = 30;
    placa = 0;
    kla = 0;
    cout << "Podaj liczbę nadgodzin:";
    cin >> nd;
    cout << "Podaj stawkę nadgodzin:";
    cin >> snd;
    if (nd <= x) {
        placa = nd * snd;
        cout << placa;
    }
    if (nd > x) {
        kla = snd * nd;
        for(3 ; 5) {
            snd = snd * 1.5
            kla = (kla + snd)
        }
        placa = 
        cout << placa;
    }
    
    
    
    
    
    
    
	return 0;
}

